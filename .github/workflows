on:
  push:
    path:
      - '**'
  pull_request:
    path:
      - '**'

jobs:
  deploy:
    name: Deploy ANsible Playbook
      if: github.event.pull_request.merged == true
      steps:
        - name: Run Ansible Playbook
          run: ansible-playbook -i hosts zerotier.yml
